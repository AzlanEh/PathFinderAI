{
  "buildCommand": "cd packages/db && npx prisma generate && cd ../../apps/web && node prebuild.cjs && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "devCommand": "npm run dev"
}
